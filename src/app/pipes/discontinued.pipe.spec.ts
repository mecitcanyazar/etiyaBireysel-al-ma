import { DiscontinuedPipe } from './discontinued.pipe';

describe('DiscontinuedPipe', () => {
  it('create an instance', () => {
    const pipe = new DiscontinuedPipe();
    expect(pipe).toBeTruthy();
  });
});
