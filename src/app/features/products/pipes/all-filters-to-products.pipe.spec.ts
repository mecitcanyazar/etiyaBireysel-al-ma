import { AllFiltersToProductsPipe } from './all-filters-to-products.pipe';

describe('AllFiltersToProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new AllFiltersToProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
